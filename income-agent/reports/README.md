# דוחות יומיים

כל ריצה יומית כותבת לכאן קובץ בשם `YYYY-MM-DD.md` עם 2–3 הרעיונות והתוכניות המלאות.

הקבצים כאן הם הפלט לקריאה. הזיכרון שהסוכן לומד ממנו נמצא ב-`../memory/`.

אחרי שקראת דוח — כתוב את התגובה שלך ב-`../memory/feedback.md`.
זה מה שמכוון את הריצה הבאה.
